# skillset-portfolio

This repo has been reset to a minimal React + TypeScript application.

## Scripts

```bash
npm install
npm start
npm run build
npm run preview
npm run deploy
```

`npm run deploy` still uses `gh-pages`, so deployment stays aligned with the existing GitHub Pages flow.
